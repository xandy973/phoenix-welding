import { LambdaContext } from './lambda/context.js';
import { InvocationMetadata, LambdaEvent } from './lambda/event.js';
export declare const AI_PROVIDERS: {
    name: string;
    apiKeyVar: string;
    baseURLVar: string;
}[];
export declare const setupEnvironment: (headers: Headers, event: Pick<LambdaEvent, "aiGateway" | "blobs">, lambdaContext?: LambdaContext, invocationMetadata?: InvocationMetadata, env?: NodeJS.ProcessEnv) => void;
