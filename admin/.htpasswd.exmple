# Ejemplo: no subir este archivo con credenciales reales al repositorio
# Genera el archivo real usando el script ../scripts/generate_htpasswd.sh
# Formato: usuario:hash
# Ejemplo (hash bcrypt o apache MD5):
# admin:$apr1$random$hashedpassword
